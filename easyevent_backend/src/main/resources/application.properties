spring.application.name=easyevent
#logging.level.root=debug

spring.web.resources.static-locations=classpath:/static/,file:uploads/

app.frontend.url=http://localhost:3000
app.max-failed-password-attempts=3
app.password.bcrypt.strength=12
app.security.tokenDurationInHours=12
upload.directory=uploads/users
upload.max-file-size-mb=5
oauth.code.ttl-s=30

# --- Datasource (PostgreSQL) ---
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=validate
#spring.jpa.show-sql=true

# --- Flyway ---
spring.flyway.enabled=true

# --- Flyway Debugging ---
spring.flyway.locations=classpath:db/migration
spring.flyway.validate-on-migrate=true
#logging.level.org.flywaydb=DEBUG

# ======================
# GOOGLE
# ======================
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}
spring.security.oauth2.client.registration.google.scope=openid,email,profile

# ======================
# MICROSOFT
# ======================
#spring.security.oauth2.client.registration.microsoft.client-id=YOUR_MS_CLIENT_ID
#spring.security.oauth2.client.registration.microsoft.client-secret=YOUR_MS_CLIENT_SECRET
#spring.security.oauth2.client.registration.microsoft.scope=openid,email,profile
#spring.security.oauth2.client.registration.microsoft.provider=microsoft

#spring.security.oauth2.client.provider.microsoft.issuer-uri=https://login.microsoftonline.com/common/v2.0

